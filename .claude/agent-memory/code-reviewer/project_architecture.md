---
name: Project Architecture & Safety Invariants
description: Core architecture, safety-critical invariants, and canonical patterns discovered during Phase 2–7 reviews + Phase 3 two-tier RAG review + food_properties migration review (2026-05-04) + factor4 clamp/audit review (2026-07-17) + A1a/A1c disclosure reviews
type: project
---

Pipeline: User Query → SemanticParser (LLM) → GroundingService (RAG + rules) → StandardizationService → ComBase engine → Result

**Why:** Safety-critical system — conservative defaults and model-type-aware bias direction are not stylistic preferences; wrong direction on thermal inactivation makes undercooked food look safe.

**How to apply:** Every review must verify both services maintain correct bias direction for all three ModelType values.

## Key Safety Invariants

### Conservative defaults (verified in settings.py)
- Temperature: `settings.default_temperature_abuse_c` = 25°C (GROWTH/NON_THERMAL_SURVIVAL), `settings.default_temperature_inactivation_conservative_c` = 60°C (THERMAL_INACTIVATION) — sourced from settings.py (Field defaults), NOT hardcoded in the service as an earlier memory version claimed; correction made 2026-08-17 after re-verifying via grep against standardization_service.py:500/518/530/880/882.
- pH: 7.0 (neutral) — from `settings.default_ph_neutral`
- Water activity: 0.99 (high) — from `settings.default_water_activity`

### Model-type-aware bias direction (standardization_service.py)
- GROWTH / NON_THERMAL_SURVIVAL: UPPER bound for ranges, +5°C bump, ×1.2 duration
- THERMAL_INACTIVATION: LOWER bound for ranges, −5°C bump, ×0.8 duration
- Low-confidence threshold for temp bump: confidence < 0.5 (strict less-than)
- Duration margin only applied when provenance.source == ValueSource.USER_INFERRED

### Range selection (grounding_service.py)
- Same model-type-aware rules applied during grounding (range bound selection)
- GroundingService handles range selection; StandardizationService handles bias correction after grounding
- Both layers must be consistent

## Provenance Tracking
- ValueSource enum (current, post Phase 3 two-tier RAG): USER_EXPLICIT, USER_INFERRED, FUZZY_MATCH, RAG_RETRIEVAL, RAG_RETRIEVAL_FALLBACK, RAG_RETRIEVAL_CATEGORY_BRIDGE, CONSERVATIVE_DEFAULT, CLARIFICATION_RESPONSE, CLAMPED_TO_RANGE, CALCULATED, MISSING
- NOTE: FUZZY_MATCH now present in metadata.py ValueSource enum (was previously missing); RAG_RETRIEVAL_FALLBACK added for Tier 2 per-field queries; RAG_RETRIEVAL_CATEGORY_BRIDGE added for Tier 3 taxonomy bridge
- attributed_field on RetrievalResult (metadata.py): None for primary queries, set to field name ("ph"/"water_activity") for Tier 2 fallback queries; drives audit routing in translation.py Priority 1 path
- No confidence numbers emitted in audit response; embedding_score (cosine similarity) is the only numeric score — categorical source tier is the reliability signal
- BiasCorrection model tracks: bias_type, field_name, original_value, corrected_value, correction_reason, correction_magnitude
- StandardizationResult is a plain class (not Pydantic) — used internally only
- `ValueProvenance.excluded_reason: str | None` (added 2026-07-17): generic "grounded but a downstream precedence rule picked something else" marker, not factor4-specific. Only ever set on the LOSING candidate's provenance, never the winner's. Surfaced on `FieldAuditEntry.excluded_reason` via `_build_field_audit()`.

## Known Pattern Issues Found in Phase 2 (some resolved)
- `_ground_food_properties()` sync call issue: RESOLVED — all retrieval calls now use `asyncio.to_thread()` correctly
- `_extract_food_properties()` ph_max bug: RESOLVED — ph_max only set when is_range=True
- `standardize()` catches bare `except Exception` at payload build — still present, should narrow
- `_extract_food_properties_llm()` catches bare `except Exception` — PARTIALLY RESOLVED: now logs warning before returning; still overbroad but no longer silent
- `LLMClient.health_check()` catches bare `except Exception` — not in scope of recent review

## Two-Tier RAG Architecture (Phase 3, 2026-04-30)
- Tier 1 threshold: food_properties_confidence = 0.70 (primary query, combined pH+aw)
- Tier 2 threshold: food_properties_fallback_confidence = 0.62 (per-field queries, calibrated)
- Tier 2 fires unconditionally for any field still ungrounded after Tier 1
- RetrievalService methods: query_food_properties (Tier 1), query_food_ph (Tier 2), query_food_water_activity (Tier 2) — all sync, called via asyncio.to_thread()
- rag_retrievals dict in _build_field_audit is keyed by query string — duplicate queries would collide; not a current risk since queries are distinct
- test_user_explicit_takes_priority: user provides pH=6.5 but NOT aw; primary returns confident result; Tier 2 aw fallback fires — mock_retrieval for query_food_water_activity NOT set up explicitly in this test (relies on MagicMock auto-return which has has_confident_result=MagicMock(), not False). This is a latent mock-completeness gap.

## Reranker Wiring (Phase N, 2026-04-30)
- CrossEncoderReranker was implemented but never injected into get_retrieval_service(); fixed so create_reranker() is called at singleton init
- create_reranker(enabled=False) returns NoOpReranker (model_name="noop") — NOT None. This means when RERANKER_ENABLED=False, NoOpReranker is still injected, it fires (truthy object), and the audit reports reranker_used="noop" + non-null rerank_scores. The reranker_used="noop" string is disclosed in the audit doc as intentional.
- Top-result threshold gate (line 175, retrieval.py): uses results[0].confidence (embedding score) NOT rerank_score. This is a known semantic gap: a reranker-promoted result below embedding threshold is rejected even if the reranker thinks it is the best match.
- Sort key mixes rerank_score and confidence in the same lambda: when reranker fires, ALL results have non-null rerank_score (NoOp assigns artificial 1.0, 0.99… scores), so the mixing issue only manifests if rerank_score is None on some results and non-None on others from the same query — which shouldn't happen in practice but could theoretically if _apply_reranker returns fewer results than n_results fetch from the store.
- Settings: reranker_enabled (default True) and reranker_model — NOT yet in .env.example
- The async-sync concern: get_retrieval_service() is called from GroundingService.__init__(), which runs synchronously. CrossEncoderReranker.__init__() loads the model synchronously (sentence_transformers). First call blocks the thread but NOT the event loop (init happens before async ground_scenario() is awaited). Not a correctness risk but a latency concern on cold start.

## Reranker Audit Restructuring (2026-04-30)
- `has_confident_result` in retrieval.py is ALWAYS set to `top_result is not None` (line 181) — the two are structurally co-invariant. `has_confident_result=True` with `top_result=None` is impossible from the production path. Tests using MagicMock can violate this (MagicMock defaults return truthy objects for all attrs), which is why _build_retrieval_metadata guards `top_result = response.top_result if response.has_confident_result else None`.
- `SkippedDocInfo.skip_reason` uses format f"failed_embedding_threshold:{threshold:.2f}" — the format is the machine-readable contract for callers.
- `runners_up` deduplication excludes both `top_id` AND `first_id` from the set. When `doc_id` is `None` on multiple results, `None` is excluded from the set (`if id_ is not None`), so all None-id docs flow into runners_up. This is a latent issue if the vector store ever returns results with null doc_ids.
- `attempted_top` test (`test_attempted_top_surfaces_when_all_failed`) only verifies that the response is HTTP 200 and `audit` is non-null — it does NOT verify that `attempted_top` is non-null in the field audit because pH source is CONSERVATIVE_DEFAULT and no retrieval block is rendered for it. The `attempted_top` path is tested at the unit layer (TestBuildRetrievalMetadata) but NOT at the API layer for end-to-end correctness.
- `_make_result_with_attempted_top` builder sets `meta.add_provenance("ph", ValueProvenance(source=ValueSource.CONSERVATIVE_DEFAULT))` — CONSERVATIVE_DEFAULT has no retrieval block, so the `attempted_top` data in meta.retrievals[0] is never surfaced to the API response body. The API test can't reach the mapping code paths for attempted_top via this mock.
- Naming convention: `RetrievalResult` exists in BOTH `app/rag/retrieval.py` (rag-layer single result) and `app/models/metadata.py` (audit record). The grounding_service import disambiguates with `from app.models.metadata import ... RetrievalResult` and `from app.rag.retrieval import RetrievalService, get_retrieval_service, RetrievalResponse`. The rag-layer `RetrievalResult` is accessed via the response object; the metadata `RetrievalResult` is written to `grounded.retrievals`. A name collision risk for future readers.
- `SkippedDocInfo` and `RunnerUpResult` share 4 identical fields (doc_id, content_preview, embedding_score, rerank_score); `SkippedDocInfo` adds `skip_reason`. They are kept separate models intentionally (different semantic roles in audit output).

## Physical Log Cap (engine.py, 2026-05-01)
- `_PHYSICAL_LOG_LIMIT = 15.0` module-level constant caps `|log_increase|` per step after `calculate_log_increase`.
- Uses `math.copysign(_PHYSICAL_LOG_LIMIT, log_increase)` — correct for both growth (positive) and inactivation (negative).
- Motivation: Salmonella thermal inactivation polynomial (model_id=2) at T=65°C (valid_ranges max) produces raw −321.9 log CFU for 10 min — unphysical. Measurability ceiling is ~12 log; 15.0 is the headroom choice.
- Cap fires per-step; `total_log_increase` accumulates already-capped values — multi-step accumulation is correct.
- Warning propagation: engine.warnings → ComBaseExecutionResult.warnings → orchestrator._execute_model (line 317: `state.metadata.warnings.extend(state.execution_result.warnings)`) → API response top-level `warnings` array via `_build_warnings_list()`.
- Test gap: `test_cap_fires_at_boundary_temperature` does NOT assert `step_predictions[0].log_increase == -15.0` (only asserts `total_log_increase`). Deferred to LOW — both values are identical for single-step; the GrowthPrediction.log_increase stores the capped value.
- `total_generations` variable initialized at engine.py line 107 but never assigned to or used — dead variable, pre-existing, not introduced by this change.
- `calculate_log_increase` for inactivation: `mu_max <= 0` branch returns `mu_max * duration_hours` (no /ln10 conversion). This is intentional — inactivation mu is already in log10/h units. For growth, the /ln10 conversion converts natural-log-scale mu to log10.

## Benchmark Suite (exp_3_3_model_comparison.py)
- Monkey-patches `litellm.acompletion` globally for token/cost tracking — not thread-safe
- Scores only first valid run against ground truth (valid[0]) — ignores consistency of accuracy across runs
- `score_extraction` range_preserved logic: accepts collapsed-to-bound as True, rejects only midpoint — correct
- Model type classification flagged as SAFETY-CRITICAL in scoring output
- `_token_log` is a module-level list mutated across async runs — race condition if concurrency ever added
- Thermal inactivation default temperature (60°C) is hardcoded in service, not configurable via settings

## Benchmark Visualization — Exp 1.1 pH Violin (Phase 2 findings, updated Phase 8 test review)

### Production code behavior (verified by probing)
- `ph_violin_chart` always adds the Scatter trace, even when all foods have `reference_ph=None` → trace has `x=[], y=[]` (empty, harmless).
- `ph_values=[]` produces a Violin trace with `x=[], y=[]` — Plotly renders nothing but trace still exists in `fig.data`. `ph_values=None` is normalized via `or []` and behaves identically.
- Missing `ph_stats` key entirely: production code uses `f.get("ph_stats", {}).get("stdev", 0.0)` — defaults to 0.0. No crash. Not tested explicitly.
- Unknown `difficulty` value: falls back to `#888888` color via `DIFFICULTY_COLORS.get(difficulty, "#888888")`. Creates its own `legendgroup`. Not tested.
- Equal-stdev foods: Python's `sorted()` is stable — insertion order preserved. Not guaranteed by spec but stable in practice.
- `food_name` missing: falls back to `"?"` string (used as x-category in Violin and in scatter x). Not tested.

### Test coverage gaps found in Phase 8 review
- `test_ph_violin_chart_food_with_no_ph_values_skipped_gracefully`: asserts `len(violins)==1` and `x==[]` but does NOT assert `y==[]`. Degenerate violin is present in trace list — callers iterating `fig.data` see it.
- `test_ph_violin_chart_food_missing_reference_ph_does_not_crash`: checks `y` of scatter but NOT `x`. If code put wrong food name in `x`, the scatter marker would appear over the wrong violin, and the test would still pass.
- `test_ph_violin_chart_empty_foods_does_not_crash`: does not check that the always-present Scatter trace has empty x/y, nor that the hline is still rendered.
- No test for `violinmode="overlay"` — a regression could silently change it to `"group"` or `"stack"`.
- No test for boundary line color (#D62728) — shape y0==y1==4.6 is checked, but color is not.
- No test for unknown/unmapped `difficulty` values (fallback color, unique legendgroup).
- No test for foods with `ph_stats` key entirely absent (exercises the `{}.get("stdev", 0.0)` fallback).
- No test that the Scatter trace is always present (even empty) — structural invariant undocumented.
- Module-level `_FOOD_*` dicts are shared references. `sorted()` in production code returns new list but references original dicts. No mutation risk today (production only reads), but no fixture isolation (no deepcopy/fixture function). If a future implementation wrote back to a food dict, all tests sharing that dict would silently pick up the mutation.

### PH_SAFETY_BOUNDARY constant
- Only used in charts.py hline; one test imports it for shape comparison. No cross-module consistency risk yet.

## Benchmark Visualization — Exp 1.1 Phase 3 (mae_by_food_chart + boundary_crossing_histogram + page sections 4 & 6)

### CRITICAL: fixture docstring/comment error in tests (HIGH severity bug)
- `_PH_SAMPLES_CROSSING = [4.3, 4.4, 4.5, 4.6, 4.7, 4.8, 4.9]`
- Comment on the line reads: `# 4 values ≤ 4.6 (4.3, 4.4, 4.5, 4.6) → 57%; 3 values > 4.6 (4.7, 4.8, 4.9) → 43%` (CORRECT in Phase 3 final code)
- Test `test_fraction_values_are_correct` docstring says `"4 of 7 samples ≤ 4.6 → 57%"` — this is CORRECT and matches production `<=` semantics.
- `test_fraction_values_are_correct` ordering assertion (idx_57 < idx_43) tests the wrong invariant: it checks
  that "57%" appears before "43%" in the merged annotation string, but when annotation text contains BOTH values,
  the index comparison depends on character position across all annotations concatenated with spaces — not on
  which side of the boundary they represent. This is a FRAGILE assertion.

### boundary_crossing_histogram: semantic mismatch between producer and visualizer
- Experiment producer (`exp_1_1_ph_stochasticity.py` line 462-467): A "crossing" is when the LLM returns a value on
  the WRONG side of the boundary relative to the food's reference pH. For acid foods (ref_ph <= 4.6), a crossing is
  a sample where v >= 4.6. For low-acid foods (ref_ph > 4.6), a crossing is v < 4.6.
- Chart visualizer (`boundary_crossing_histogram`): counts n_below = sum(v <= 4.6) and labels it "≤" side, n_above = > side.
  This is a fixed split regardless of which side is "wrong". The fractions shown are therefore NOT the same as
  the boundary_crossing_rate from the experiment — they are a raw split. For low-acid foods, the "below" fraction
  IS the crossing fraction; for acid foods, the "above" fraction is the crossing fraction.
- Impact: the histogram annotation is not wrong per se (it shows a neutral split), but could be misleading.
  Not a correctness bug in the chart math, but a documentation/label gap.

### Page Section 6: st.columns(min(len(crossing_foods), 2)) with 1 food
- `st.columns(1)` returns a list with one element. `cols[0 % 2]` = `cols[0]` — works correctly. No crash.
- Verified safe.

### Page Section 6: first-model-wins deduplication
- Deduplicated by food_name, first model wins. Means if Model A and Model B both have "salsa" with different
  ph_values (different stochasticity), only Model A's salsa histogram is shown. This may hide variance differences.
  Documented as a design choice to verify, not a bug.

### mae_by_food_chart single-model path: silent drop of unknown difficulty
- Foods whose difficulty is not in ("easy","medium","hard") are silently dropped. No warning, no fallback trace.
- Documented in test `test_food_with_unknown_difficulty_is_silently_dropped`. Acceptable if intentional.

### Assertion weakness patterns confirmed in Phase 3 tests
- `test_mae_values_correct`: checks only sorted y-values across all traces — x-label swap bugs would pass
- `test_fraction_annotation_present`: checks for `%` symbol only — passes even on `0% / 0%` output
- `test_reference_ph_line_present_when_provided` in boundary histogram: assertions on color (black) and dash style
  ARE present in Phase 3 (lines 988-989) — previously noted gap is now covered
- `test_all_samples_below_boundary`: asserts `"100%"` is present AND `"0%"` is present — both sides checked
- `test_all_samples_above_boundary`: added in Phase 3, covers the previously noted gap

### Coverage gaps remaining after Phase 3
- No test for multiple foods within the same difficulty tier (food-name ordering within a tier)
- No test for multi-model with different food sets per model (mismatched x-axis per model)
- No test for `reference_ph == PH_SAFETY_BOUNDARY` (both vlines at same x=4.6)
- No test for `showlegend=False` on the histogram chart layout
- No page-level test for Section 6 deduplication logic (first-model-wins behavior)
- No page-level test for `st.columns(1)` single-crossing-food layout

### Safe patterns confirmed
- `add_vline` does produce shapes with `x0 == x1` — vline detection idiom in tests is correct
- `add_hline` produces `y0 == y1 == value` — MAE threshold detection in tests is correct
- `legend_title_text` in `update_layout` maps to `layout.legend.title.text` — legend title assertions correct
- `_make_results` now uses `copy.deepcopy` — fixture isolation is correct
- `load_latest_results` now guards against non-list JSON with isinstance check before returning — previous
  Phase 7 finding about malformed JSON is resolved in data_loader.py

## Benchmark Visualization (Phase 7 findings)
- `load_latest_results` return type annotation says `list | None` for first tuple member but actually returns
  whatever `_load_json_safe` returns — which is `dict | list | None`. Malformed JSON as a top-level dict
  would cause `for r in results` and `len(results)` on page 2 to iterate/count dict keys.
- Page 3 `get_query_count` already guards with `isinstance(results, list)` — page 2 does not.
- `st.sidebar.page_link` in app.py is called BEFORE `st.navigation([...])` — may produce Streamlit warnings
  or fail in strict navigation mode.
- `_fmt_run_ts` in app.py truncates seconds from the timestamp label (shows HH:MM, not HH:MM:SS),
  inconsistent with `_format_timestamp` in overview page and page 3 which shows full HH:MM:SS.
- Stale `selected_run` in session_state: if runs exist, user picks one, then results are deleted, the
  session key is never cleared — page 2 calls `load_run_by_timestamp` with stale ts, gets (None, None),
  shows "No results found" rather than falling back to latest.
- Colorscale `0.6999` step approach is semantically correct but the 0.0001 gap is tighter than necessary;
  a value of exactly 0.7 maps to position 0.7 which hits the amber entry correctly.
- `test_field_accuracy_heatmap_colorscale_has_exactly_six_stops` is fragile — any stop addition breaks it.
- test_viz_model_comparison.py mirrored helper is identical to the production expression — if the page
  expression changes without updating the mirror, tests continue to pass against the stale copy.

## Audit Completeness Fix (2026-05-07): MISSING source tier + per-step provenance bridging
- `ValueSource.MISSING` added — sentinel for fields that appeared in `missing_required` but had no provenance
- Bridging loop in `orchestrator._ground_values()` writes per-step temp/dur provenance into `state.metadata.provenance` AND step-qualified keys into `state.grounded_values` — both required for `_build_field_audit` to resolve `final_value`
- Guard `if audit_key not in state.metadata.provenance` in the `missing_required` loop prevents overwriting a real grounding provenance with MISSING — correct for the happy path, but has a gap when `dur_provenance is None` (grounding returned `(None, None)`) AND the bridging loop correctly skips that key. In that case the key is absent from `provenance`, so MISSING fires — the correct outcome.
- `_missing_key_to_audit_key()` covers the 3 known field spec forms: "duration" → "duration_minutes", "duration (step N)" → "duration_minutes (step N)", "temperature" → "temperature_celsius". Unknown specs fall through as identity. (Post-2026-07-17: also covers organism-not-executable and factor4-no-declared-range specs — see below.)
- Defect: "temperature_celsius" is never emitted by standardizer (temperature gets a default before missing fires) — the test `test_single_step_temperature` tests the mapping but not a real standardizer path; the mapping is still correct as a defensive fallback.
- Integration test `test_missing_duration_in_field_audit_with_null_value` relies on real grounding resolving `description=""` → `(None, None)` from `_resolve_duration_value`; this is correct since the fixture uses `ExtractedDuration()` with no value and no description.
- `standardization_service=None` in unit fixture triggers `get_standardization_service()` which calls `get_combase_engine().registry` at first call — the singleton chain reaches the real CSV loader. Safe because `data/combase_models.csv` is present in the repo.
- Partial-standardization events (defaults_imputed, range_clamps, warnings) now recorded before the `missing_required` early return — this was the core bug fix.

## Vague Temperature Workstream (2026-05-07): rule library + prompt guard

- `find_temperature_interpretation` sorts by `len(r.pattern)` descending at call time — physical list order is irrelevant.
- Substring match returns the original `InterpretationRule` object with `similarity=None`. Embedding fallback in `find_temperature_interpretation_with_fallback` creates a NEW `InterpretationRule` with `similarity=<float>` set. Tests assert `rule.similarity is None` to confirm substring path, not embedding path.
- `conservative=False` on all 11 new refrigeration rules is CORRECT: refrigeration temperature (4°C) is not a conservative upper bound for growth — it is the standard setpoint that represents the low end of the growth concern range. `conservative=True` is reserved for rules that commit to the pessimistic end of an uncertain range (e.g., "cold" → 10°C, "room temperature" → 25°C).
- Comment at rules.py line 175 states `"typical retail refrigeration"` is 29 chars — actual length is 28. Minor doc inaccuracy only, no functional impact.
- `@pytest.mark.live` is registered in `pyproject.toml` but NOT excluded from default runs by `addopts` — must be passed as `-m "not live"` to exclude. This is a CI configuration gap: if CI runs `pytest` without a marker filter, live tests will fail on missing API keys.
- The Set C integration test fifth parametrize case (`"Sauce held at 4°C for 6 hours"` expecting `user_explicit / llm_extraction`) is a correct regression sentinel: it confirms numeric temperatures are not over-stripped to descriptive form.
- `find_temperature_interpretation_with_fallback("a bit warm")` DOES resolve — matches the substring rule `"warm"` → 30°C. Worth checking against the real rule library before assuming a phrase is "unresolvable" for test/example purposes (an illustrative phrase in a task prompt is not verified data — confirmed as a named lesson in the A1c 2026-08-17 entry below). `"xyz123"` is the established go-to fixture string for a genuinely unresolvable temperature description across this test suite.

## Tier 3 FoodEx2 TaxonomyBridge (2026-05-07 initial + 2026-05-08 curated-row restriction)
- Pipeline: TaxonomyBridge resolves food_description → ptm_category via fuzzy matching against food_taxonomy.csv (2917 FoodEx2 MTX entries), then reads category_level_rows.csv (5 curated rows) for that category — no longer envelopes all food_properties rows.
- Composite-food blocklist runs BEFORE fuzzy match — structural order is pinned by monkeypatch test.
- Alias map (beef→bovine, pork→pig, mutton/lamb→sheep, veal→calf) applied BEFORE fuzzy match.
- Composite scoring: (token_set_ratio, fuzz.ratio) tuple — tiebreaker prevents short queries from matching longer taxonomy entries first in CSV order.
- Default threshold: 80 (gap between max true-negative ~57 and min true-positive ~90.9 = 33.8 pts).
- Post-2026-05-08: Tier 3 now uses `lookup_category_level_row(category, state, field)` keyed by (ptm_category, state, "ph"/"aw") — no envelope across all property_rows. Only 5 explicitly curated rows in data/rag/category_level_rows.csv are eligible.
- `TaxonomyResolution.matched_state` carries raw 'state' column from winning taxonomy row. `_apply_state_default` converts "unspecified"/"" → "fresh". `CategoryBridgeInfo.assumed_state` is non-empty only when conversion happened.
- `bridge_attempts` populated when lookup_category_level_row returns None; StandardizationService reads bridge_attempts["ph"] and bridge_attempts["water_activity"] for DefaultImputed reason text — this consumer IS now implemented (standardization_service.py lines 446-454 and 529-537).
- Sentinel pattern: `_BRIDGE_SENTINEL = object()` at module level; `taxonomy_bridge=_BRIDGE_SENTINEL` default reads env var; explicit `None` disables unconditionally; explicit instance uses it unconditionally.
- ENV VAR: `PTM_TAXONOMY_BRIDGE_ENABLED` — unset or truthy → enabled; "0"/"false"/"no" → disabled; empty string → disabled (distinct from unset — intentional).
- Startup validation in main.py raises FileNotFoundError for ALL four bridge CSVs regardless of env-var state. This means setting PTM_TAXONOMY_BRIDGE_ENABLED=false does NOT allow a deployment without category_level_rows.csv.
- `_index_category_level_rows` correctly handles rows with only aw (empty ph_min → no "ph" key) and rows with only ph (empty aw_min → no "aw" key). No false index entries.
- curated_row["food_name"] access at grounding_service.py:725 is safe — _index_category_level_rows preserves all CSV columns via `{k: v.strip() for k, v in row.items()}`.
- eggs row: aw_min=aw_max=0.97 (point estimate, not a range). val_min == val_max → range [0.97, 0.97] stored with range_pending=True. StandardizationService will select 0.97 as the upper bound for GROWTH — correct.

## Category-Level Pathogen Fallback (2026-05-14)
- New fallback in `_category_pathogen_fallback()` fires when two-stage RAG pathogen lookup yields no confident result (3 paths: Stage 1 below threshold, Stage 2 empty, Stage 2 from_text unmapped).
- Pipeline: food_description → TaxonomyBridge.resolve() (threshold 80) → ift_category_alignment.csv → union of pathogens from pathogen_food_associations.csv → rank by annual_deaths_us from pathogen_characteristics.csv → top ComBaseOrganism-mappable candidate.
- `_PATHOGEN_NAME_NORMALIZATION` dict: used ONLY for characteristics lookup (e.g., "salmonella spp." → "salmonella nontyphoidal"). `from_text()` always receives the original associations-CSV name (e.g., "Salmonella spp."), not the normalized name. Correct by design.
- Deduplication (seen_lower set) is applied before ranking. Pathogens absent from characteristics are excluded entirely; zero-death pathogens are ranked last, not excluded.
- `condiment` and `beverage` rows in ift_category_alignment.csv have empty ift_category — _load_ift_alignment correctly skips them (both ptm and ift must be non-empty). Fallback behavior: .get("condiment") → None → "not ift_categories" → early return. Correct.
- Three loader methods (_load_ift_alignment, _load_pathogen_associations, _load_pathogen_characteristics) are called at __init__ unless test-injected dicts are passed. Missing CSV files log a warning and return {} — fails closed.
- ValueSource.RAG_PATHOGEN_CATEGORY_FALLBACK: new enum member. NOT included in is_rag_source check in translation.py (line 123) — so no retrieval block is rendered for the organism when this source fires, even though a Stage 1 miss is in grounded.retrievals with attributed_field="organism". This is a medium audit gap — consistent with how RAG_RETRIEVAL_CATEGORY_BRIDGE is also excluded from is_rag_source.
- RESOLVED (verified 2026-08-17, A1c review): `grounded.warnings` (including the transparency warning emitted by `_category_pathogen_fallback`) IS now propagated to `state.metadata.warnings` — `orchestrator._ground_values()` lines 856-857: `if grounded.warnings: state.metadata.warnings.extend(grounded.warnings)`. This directly resolves the CRITICAL GAP previously noted here (pre-existing TODO at old orchestrator.py line ~416); the fix must have landed in a phase between 2026-05-14 and 2026-08-17 review. Do not re-flag this as an open gap in future reviews without re-checking — the propagation is real and is exactly the mechanism the A1c disclosure work (see A1c entry below) built on top of.
- ift_categories list in PathogenCategoryFallbackInfo is a direct reference to self._ift_alignment[ptm_category] — aliased, not copied. Safe in current use (read-only after grounding).
- `test_no_ift_mapping_returns_without_grounding` in unit tests injects `{"condiment": []}` (explicit empty list) but production state is `{}` (key absent). Both paths trigger `not ift_categories` → early return. Behavior matches, but comment "explicit empty" is misleading vs production reality.
- Unit test `test_category_fallback_when_stage2_empty` in test_grounding_service.py uses production CSVs (grounding_service fixture does not inject tables) — it is a data-dependent "unit" test that belongs logically in integration tests.

## Organism Grounding Failure Record — Phase A0 (2026-07-15)
- New `OrganismGroundingFailure` (metadata.py) + `OrganismGroundingFailureStage` enum (enums.py): structured audit twin of `PathogenCategoryFallbackInfo`, but for the failure side. Attached to `GroundedValues.organism_failure`, default `None`.
- CONFIRMED: `organism` has NEVER had a code-level default — `standardization_service._get_organism()` just returns `grounded.get("organism")`, no fallback, and `settings.py` has no organism-related setting at all. Reviewed and verified this via grep; it's not new in this change.
- IMPORTANT DOC CORRECTION: CLAUDE.md, specs/specifications.md, and docs/ptm_context.md previously claimed "Organism → Salmonella" was a conservative default. This was **stale documentation describing a default that never existed in code** — no test asserted it either. Fixed in this same diff. See `specs/lessons.md` 2026-07-15 entry "the Salmonella default that never existed" for the full lesson (documented conventions != code; grep for the concrete constant + a test before trusting a documented default).
- Also flagged in lessons.md: a "scaffolded-but-unwired" pattern — `ClarificationRecord`, `add_clarification()`, `ExtractedClarificationResponse`, `SessionStatus.AWAITING_CLARIFICATION`, `ValueSource.CLARIFICATION_RESPONSE`, `extract_scenario(conversation_context=)` are all defined/tested in isolation but NOT reachable from `orchestrator.translate()`. Worth checking reachability from the orchestrator before assuming any of these subsystems is live in a future review.
- `_category_pathogen_fallback()` has exactly 6 early-return points, verified 1:1 matched to 6 `OrganismGroundingFailure` constructions (grounding_service.py lines ~1264-1359): BRIDGE_DISABLED, FOOD_UNRECOGNISED, CATEGORY_HAS_NO_HAZARD_DATA (only stage populating resolved_category/match_score from `resolution.match_score`, sourced correctly), and 3x INTERNAL_NO_MAPPABLE_CANDIDATE (empty candidate union, all candidates absent from characteristics, all ranked candidates unmapped) — deliberately collapsed into one enum member since indistinguishable to any consumer, disambiguated only by free-text `detail`.
- Success paths (user-explicit organism at line 577 `not grounded.has("organism")` guard, Stage-1 RAG confident hit, Stage-2 RAG hit) never touch `organism_failure` — it stays at its `None` default correctly. Only `_category_pathogen_fallback()` ever writes to it.
- `GroundedValues.__init__(self)` takes no external args (plain class, not Pydantic) — safe to add new attributes with in-body defaults without touching any of the 8 call sites across tests/production that construct `GroundedValues()`.
- Test coverage gap found: `test_no_ift_mapping_returns_without_grounding` in `tests/unit/test_category_pathogen_fallback.py` exercises the exact same CATEGORY_HAS_NO_HAZARD_DATA branch as integration test N2 (mustard) but was NOT given the new `organism_failure`/`resolved_category`/`match_score` assertions that its two sibling tests in the same file received. Coverage exists only via the integration test (which uses the real TaxonomyBridge against real CSVs) — no isolated unit-level assertion of resolved_category/match_score population. Minor, non-blocking.
- `make_service()` test helper in test_category_pathogen_fallback.py defaults `taxonomy_bridge=None` → constructs a REAL `TaxonomyBridge()` reading actual CSVs (not a mock) when the param is omitted — despite the module docstring claiming "no filesystem reads". Only tests that explicitly pass `taxonomy_bridge=None` as an override in the GroundingService constructor call (bypassing make_service's own default-swap) get the fully synthetic path. Worth noting for future reviews of this file.

## food_properties.csv Migration (2026-05-04): per-field source columns
- Migrated from single `source_id` column + notes-parsing workaround to dedicated `ph_source_id` / `aw_source_id` columns
- `EXPECTED_FOOD_PROPERTIES_COUNT = 252` constant in food_safety.py (after 2026-04-17 audit removed 7 rows)
- `_valid_source_ids()` is `lru_cache(maxsize=1)` — tests bypass via `monkeypatch.setattr(fs, '_valid_source_ids', lambda: ...)`. This works because the bare-name call inside `load_food_properties` resolves via the module's `__dict__`, which monkeypatch modifies directly.
- Validation order: empty-source check fires BEFORE unknown-source check (correct — empty source cannot be unknown).
- `row_idx = records - 1` in error messages: this is 0-indexed DATA row count (comment-skipped rows excluded), NOT CSV file line number. If comment rows precede the error row, row_idx will not match the rag_audit_changelog "Row (original)" column.
- Sanity check placement: after the loop, before `return` — correct.
- No rows exist in current CSV with neither ph nor aw data; no whitespace-only source IDs.
- `valid_ids = _valid_source_ids()` is called inside the loop (once per row). With lru_cache, the cost is one frozenset lookup per call — negligible but could be hoisted to before the loop for clarity.
- `test_notes_field_does_not_contribute_to_source_ids` IS a genuine proof: IFT-2003-T31 appears in notes but NOT in ph_source_id/aw_source_id; the test confirms it is absent from stored source_id. Would fail if notes were parsed.
- Source ID validation when source_references.csv is missing: `_valid_source_ids()` returns `frozenset()`, causing any non-empty source ID to fail validation. Correct fail-safe behavior.
- chunk_size=512, food_properties docs are ~90-150 chars → always 1 chunk per row. Comment in sanity check about "food_properties_0..food_properties_251" IDs is accurate for current data but fragile: depends on 1 chunk per row assumption.

## Experiment 2.1 Embedder Comparison (2026-05-15)

### Architecture
- Benchmark writes per-embedder ChromaDB to `benchmarks/results/exp_2_1_embedder_comparison/_chroma/<embedder_id>/` — never touches production `data/vector_store/`.
- JSON output format: `{"experiment_id":..., "timestamp":..., "corpus":..., "embedders":[...]}` (dict, NOT list). The shared `load_latest_results()` in `data_loader.py` rejects non-list JSON. Streamlit page 5 has its own `_load_latest()` that handles dict format correctly.
- Query reformulation strings in `_FIELD_TO_SUFFIX` match production `RetrievalService` verbatim (confirmed against `retrieval.py`).
- Pathogen filter: `{"$and": [{"type": "pathogen_hazards"}, {"data_type": "food_pathogen_hazard"}]}` — matches `VectorStore.query()` multi-key construction.
- Food properties filter: `{"type": "food_properties"}` — single key, plain dict.

### Known issues found in Phase 2.1 review
- `_query_collection` (line 533) catches bare `except Exception: return []` — silences ChromaDB errors including filter mismatches that would indicate a bug in `_build_where_filter`.
- `embed_corpus` (line 479) catches bare `except Exception: pass` for `delete_collection` — acceptable because the collection may not exist on first run; the bare except is intentional here.
- `_get_threshold` keys ("tier_1", "tier_2", "pathogen_stage_1") must stay in sync with corpus JSON `production_tier` values. The mapping is the single source of truth; changing settings variable names would silently break threshold lookup if tests only check against live settings.
- `async def main()` contains no `await` calls — unnecessary async declaration (NIT).
- `test_tier_2` and `test_pathogen_stage_1` in unit tests compare `_get_threshold()` against literal values (0.62, 0.75) rather than only against `settings.*`. If settings defaults change, tests would catch the mismatch — but framing as "reads from settings" while using literals is slightly misleading.
- `latencies` list in `_compute_embedder_summary` (line 275) always has length == `n` (same list comprehension, with `.get("latency_ms", 0)` fallback), so `if latencies else None` on line 283 is dead code. Harmless.
- `correct_at_3 = correct_at_1` for negative controls is logically correct: results sorted descending, so if top-1 is below threshold, all top-3 are too.

## Fail-Closed Organism Executability — Phase A0.5b (2026-07-16)
- `StandardizationService.standardize()`'s `is_executable()` check (registry.is_executable, gating on `organism`+`model_type`+`factor4_type`) sits BEFORE the `if grounded.has_steps:` branch that dispatches to single-step vs `_build_multi_step_profile()`. This means it protects BOTH paths despite living in code that reads like a single-step-only guard — worth re-confirming placement (not just presence) on any future review that touches this method's control flow, since moving the check inside either branch would silently reopen the other.
- `registry.get_model()` (execution path, keys off `organism.value` directly) and `registry.get_models_for_organism()` (keys off `_by_organism`, populated via `ComBaseOrganism.from_string()` at CSV-load time) are two DIFFERENT lookup mechanisms that agree only because no one currently edits `.value` without also updating `_get_fuzzy_map()`. An invariant test that walks one path reads as protection for the other but isn't — confirmed by deliberately desyncing them (see lessons.md 2026-07-16 entry). When reviewing new registry-lookup code, check which of the two mechanisms it actually uses.
- `sf` (Shigella flexneri, `ComBaseOrganism.SHIGELLA_FLEXNERI = "sf"`) is the one organism with no `(ModelID=1, Factor4=NONE)` row in `data/combase_models.csv` — its only row is `ModelID=1, Org="Shigella flexneri with nitrite(ppm)", Factor4=nitrite`. Useful as the canonical non-executable-combo test fixture; verified directly against the CSV, not just trusted from a prior description.
- `_organism_display_name()`'s enum-name fallback (`organism.name.replace("_","").title()`) is reachable only if `get_models_for_organism()` returns zero rows for an organism that DID pass `is_executable()` via `.value` — i.e., only under the exact from_string/`.value` desync described above. Currently unreachable given the two CSV-invariant tests, but correctly defensive rather than dead code, since it guards a real (if currently test-blocked) mechanism gap, not a hypothetical one.
- Pattern for "no existing test modified" claims: a diff can leave every assertion/body byte-for-byte identical while still editing an existing test's DOCSTRING (seen in `test_organism_value_resolves_to_matching_csv_row`, A0.5b). This satisfies the spirit of the constraint (behavior/coverage unchanged) but not a literal reading. Worth calling out precisely rather than either failing the check or silently waving it through.
- `missing_required` in `StandardizationResult` is a short, closed list of producers (grep for `missing_required.append` — as of A0.5b, 4 call sites: ungrounded organism, non-executable organism, missing single-step temperature [defensive/near-unreachable], missing multi-step step duration; as of the 2026-07-17 factor4 commit, a 5th call site was added — see below). `_missing_key_to_audit_key()`'s string-prefix/substring matching is safe specifically because this list is closed and reviewable in one grep — re-verify the producer count hasn't grown before trusting a prefix-match audit-key mapping in a future review.

## Factor4 Clamp/Audit + Fail-Closed on Missing Bounds — 2026-07-17 (commits 8784377, d7b784d)
Reviewed and verified clean; no findings above LOW severity.

- **Commit 8784377** (mechanical refactor): extracted `_clamp_to_constraints()` shared helper from three hand-rolled clamp blocks (`_get_temperature`, `_get_ph`, `_get_water_activity`). Diffed old-vs-new by hand: warning string templates and `RangeClamp` field values are byte-for-byte identical; the only behavior-relevant change is `if constraints and not constraints.is_X_valid(v):` becoming `if constraints: v = self._clamp_to_constraints(...)` where the helper internally no-ops (`return value` unchanged) when `is_valid(value)` is True — same net effect. No test file touched in this commit (confirmed via `git diff --stat`, tests/ empty). A fourth hand-rolled instance (`_build_multi_step_profile()`'s per-step temperature clamp) was deliberately left un-extracted (documented, out of stated 3-call-site scope) — worth checking on a future pass since it's now the only clamp site not using the shared helper.
- **Commit d7b784d** (factor4 clamp/audit + fail-closed): `_get_factor4()` split into pure `_pick_factor4_candidate()` (reads only `grounded.has()`/`grounded.get()`, no side effects — confirmed safe to call twice in the same `standardize()` invocation; nothing mutates `grounded` between the two calls) and the impure `_get_factor4(grounded, result, constraints)` (warns+marks exclusions, fails closed on missing bounds, clamps via the Commit-1 helper). `standardize()` calls the picker once early (bare peek, just for `factor4_type` to feed `is_executable()` — constraints don't exist yet) and once more via the full `_get_factor4()` after constraints resolve; this is a genuine ordering constraint (constraints depend on factor4_type, factor4_type comes from the picker), not an oversight.
- **Fail-closed bounds check correctly distinguishes** "no factor4 at all" (factor4_type == NONE, model row's Factor4ID is genuinely NULL) from "factor4 declared but bounds missing" (factor4_type != NONE but `constraints.factor4_min`/`factor4_max` is None): the `factor4_type == Factor4Type.NONE` early-return happens BEFORE the bounds check, so normal no-factor4 predictions never touch `is_factor4_valid()`'s permissive `True`-when-bounds-absent fallback. Verified by tracing `_get_factor4()` line by line, not just trusting the commit message. `is_factor4_valid()` returning `True` for `(None, None)` bounds is a legitimate "N/A" case for factor4_type==NONE and a dangerous fail-open case for factor4_type!=NONE with missing bounds — the new code adds the missing-bounds check for the latter case *before* ever calling `is_factor4_valid()`.
- **`excluded_reason` only ever written to the LOSING candidate(s)' provenance** (`grounded.provenance[excluded_field]`), never the winner's — confirmed by reading the loop (`for excluded_field in excluded_fields: ...`, `excluded_fields = present[1:]`, winner is `present[0]` and never appears in that list). When only one factor4 candidate is grounded, `excluded_fields == []`, the `if excluded_fields:` guard is False, and no provenance is touched anywhere — `excluded_reason` correctly stays at its `None` default. `GroundedValues.set()`/`set_with_prov()` both unconditionally populate `self.provenance[field]` whenever `self.values[field]` is set, so the `if excluded_prov is not None` defensive check in the exclusion loop is currently unreachable dead code (same "defensive but provably unreachable given a closed invariant" pattern noted for `_organism_display_name()`'s enum-name fallback in the A0.5b entry above) — not a bug, just worth knowing it's unreachable rather than assuming it's live.
- **`prov.standardization` mechanism claim re-derived independently, not trusted from the commit message**: grepped all 6 `prov.standardization =` / `*_prov.standardization =` assignment sites in `standardization_service.py` — every one assigns a `RangeBoundSelection` object from `_select_range_bound()`; none ever assign clamp data. `_build_field_audit()` (`app/api/routes/translation.py` ~line 88-112) builds `clamp_by_field` from `metadata.range_clamps` and checks `if field_name in clamp_by_field: ... elif prov.standardization is not None: ...` — confirmed this is a read-priority at the route layer, not a write/overwrite in the service layer. The specs/specifications.md and lessons.md corrections in this commit accurately describe this.
- **Verification numbers, independently reproduced (not trusted from commit message)**: `pytest -q` → 1321 passed, 41 deselected (1312 baseline + 9 new tests: 8 in `test_factor4_standardization.py` + 1 in `test_orchestrator.py::test_factor4_no_declared_range` — arithmetic checks out exactly). `mypy app` → 151 errors, identical count and identical file/line set at both `8784377` and `d7b784d` (checked out both commits and ran mypy fresh at each to confirm zero net-new errors, not just diffed line ranges). `ruff check` on the touched files → 14 findings, all traced to lines outside both commits' diff hunks (translation.py:532 docstring whitespace, translation.py:639 B904 in an unrelated except clause, orchestrator.py:17-35 E402 import-order at module top, orchestrator.py:412 SIM102 in an unrelated nested-if, metadata.py:30 UP042 enum-inheritance style predating `excluded_reason`). `data/combase_models.csv` and `app/engines/combase/engine.py` both confirmed byte-identical across the full two-commit diff range (`git diff 8784377^..d7b784d -- <path>` empty for both) — the "no CSV edits" and "no engine.py change" non-goals both hold.
- **Minor observations, not findings**: the excluded-candidate warning is still appended even when the request ultimately fails closed on missing bounds (warnings + missing_required both populate before the early return) — harmless since a `missing_required` result short-circuits to a 4xx/failure response that likely doesn't surface `warnings` anyway, but worth a glance if warning-surfacing behavior on failed requests ever changes. `label=field_name.replace("_", " ")` produces slightly awkward warning text ("nitrite ppm" instead of "Nitrite (ppm)") — cosmetic only.

## Organism Clarification Gate — Phase A1a (2026-07-16, ask-only)
Reviewed and verified clean; findings were LOW/MEDIUM only (encapsulation smell + one hardcoded fact in generated text), no CRITICAL/HIGH.

- New pieces: `ClarificationReason.ORGANISM_FOOD_UNRECOGNIZED`/`ORGANISM_CATEGORY_UNCOVERED` (enums.py), `ClarificationOption`/`ClarificationQuestion` (metadata.py), `app/services/clarification/clarification_service.py` (NEW, pure/deterministic, no I/O), `GroundingService.rank_executable_organisms()`, `StandardizationResult.factor4_type`, `Orchestrator._build_organism_clarification()`, `SessionState.clarification_question`, `TranslationResponse.clarification`.
- **Trigger is genuinely narrow**: `standardize()` returns immediately after `result.missing_required.append("organism")` (bare string, no other entries can co-occur), so `"organism" in std_result.missing_required` cannot false-positive against `"organism (Shigella flexneri is not supported for growth predictions)"` (the A0.5b non-executable case — list membership is exact string equality, not substring). Confirmed via `TestShigellaExecutability` staying untouched and still asserting `result.error is not None` / hard failure.
- **`_pick_factor4_candidate()` reordering (standardization_service.py) confirmed genuinely pure** by reading the method body directly: reads only `grounded.has()`/`grounded.get()` over a fixed candidate list, no registry/organism access, no mutation. Safe to call once as an early unconditional peek (to populate `result.factor4_type` even on the organism-missing early return) and again later in `_get_factor4()` after constraints resolve — same grounded data, same winner both times, side effects (warnings, `excluded_reason` writes) only fire on the second (real) call, never the peek.
- **Empty/malformed option set correctly falls back to unchanged failure**: `ClarificationService.build_organism_question()` itself does NOT gate on empty `ranked_organisms` (a test explicitly proves it still returns a question with just the free-text escape) — the emptiness gate lives entirely in `Orchestrator._build_organism_clarification()`'s `if not ranked: return None`. Two-layer design: service is permissive/pure, orchestrator owns the "is this worth asking" decision.
- **No short-code leakage**: `ClarificationOption.code` deliberately carries the `ComBaseOrganism.value` short code (e.g. `"ss"`) as a separate structured JSON field; `label` always goes through `StandardizationService._organism_display_name()` for human-readable text. Consistent with the Shigella-error-message convention (short codes never appear inside human-readable question/label strings) — the short code is a legitimate machine field here, not a leak.
- **MEDIUM finding**: `Orchestrator._build_organism_clarification()` calls `self._standardizer._organism_display_name(organism)` — a leading-underscore "private" method of a different service, called across a module boundary. Only cross-service private-method access in orchestrator.py. Not a functional bug (verified correct output) but an encapsulation smell; a future rename/refactor of that method would break silently (no import-level signal). Suggest de-underscoring it or extracting to a shared display-name helper if a third caller ever appears.
- **MEDIUM finding**: `ClarificationService`'s `CATEGORY_HAS_NO_HAZARD_DATA` preamble hardcodes the literal claim "covers 8 food categories" in generated user-facing text. Independently verified accurate against `data/rag/ift_category_alignment.csv` (exactly 8 distinct non-empty `ift_category` values) at review time, but the number is a string literal, not derived from `len(set(...))` at call time — if the CSV changes, this text silently goes stale. The only test coverage checks the literal `"8"` is present (tautological, not independently computed). Low blast radius (informational text only, not a safety default) but worth deriving from data or genericizing the wording in a follow-up.
- **LOW/NIT**: spelling inconsistency introduced in this diff — new `ClarificationReason.ORGANISM_FOOD_UNRECOGNIZED` uses American spelling while the pre-existing (Phase A0) `OrganismGroundingFailureStage.FOOD_UNRECOGNISED` it maps 1:1 from uses British spelling. Same feature area, same diff area (A0→A1a), inconsistent spelling convention.
- **API contract**: additive-only (`TranslationResponse.clarification: ClarificationInfo | None = None`, `SessionState.clarification_question`). New response shape: `success=False, status=AWAITING_CLARIFICATION, error=None, prediction=None, clarification={...}` — `error` stays `None` because `state.set_error()` is never called on this path (uses `state.update_status()` instead). Verified this is a deliberate, coherent tri-state (not a `success=False`-implies-`error≠None` regression) by reading `TranslationResult.__init__` (`success = status == COMPLETED`, `error = state.error` verbatim, no coupling between the two).
- Verification (independently reproduced, not trusted from the implementer's numbers): `pytest -q` → 1337 passed, 41 deselected (1321 baseline + 16 new tests, arithmetic checks out). `mypy app` → 151 errors, same count as the immediately-prior factor4 review with no new errors on touched lines (confirmed the one claimed self-fix — importing `ClarificationReason` from `app.models.enums` not `app.models.metadata` — is correct). `ruff check` on touched files only → all findings traced to pre-existing lines outside this diff's hunks (orchestrator.py E402 import-order/SIM102, enums.py UP042/SIM118, metadata.py UP042, translation.py W293/B904). `black --check` clean repo-wide.
- Test quality note: `test_derived_against_real_csv_and_registry` (test_grounding_service.py) is a "mirrored helper" pattern (same pattern flagged for `test_viz_model_comparison.py` in an earlier review) — it recomputes the expected ranking by re-executing essentially the same algorithm as the production code, so it verifies protection against CSV/registry data drift but does NOT independently verify the ranking algorithm's correctness (a logic bug shared between production and test would not be caught). The other five tests in `TestRankExecutableOrganisms` use hand-constructed fixtures and do provide real correctness coverage — this one is supplementary, and its docstring is honest about that scope, so not a defect, just a pattern worth naming.

## Advisory Disclosure Workstream A1c — 2026-08-17 (model-type fall-through + temperature-ungrounded wording)
Reviewed and verified clean at CRITICAL/HIGH; findings were MEDIUM/LOW only. Full test suite (1376) passes per implementer, independently reran the 10 new/targeted tests (all pass) plus ruff/black on the 3 touched production files (0 new findings, all pre-existing baseline noise).

- **Files touched**: `app/core/orchestrator.py` (model-type fall-through disclosure — new `_MODEL_TYPE_FALLTHROUGH_REASON` module constant shared between the branch-8 return statement and an exact-equality check right after `_determine_model_type()` returns), `app/services/grounding/grounding_service.py` (`_ground_temperature()` wording polish only — two `mark_ungrounded()` messages reworded into full sentences), `app/api/routes/translation.py` (`_build_warnings_list()` gains a `_TEMPERATURE_UNGROUNDED_PREFIX = "temperature_celsius: "` string-prefix check that splits the field name out into `WarningInfo.field` structurally, matching how the range-clamp branch already does it).
- **Case 1 guard is genuinely exact-match, not enum-value-match**: traced all 8 branches of `_determine_model_type()` (orchestrator.py:360-397) — each of branches 1-7 returns a distinct, non-fall-through reason string (static or f-string), and critically, branches 1 (explicit param) and 2 (LLM inference) CAN both return `ModelType.GROWTH` with a different reason string. The new guard is `if model_type_reason == _MODEL_TYPE_FALLTHROUGH_REASON` — string equality against the reason, never a check on the resulting `ModelType` — so confident `GROWTH` decisions never false-fire the "this is a guess" warning. This is the one part of this workstream that would have been a real correctness bug if done wrong (crying wolf defeats the entire feature per the implementation prompt's own framing), and it's implemented correctly.
- **Propagation confirmed genuinely unconditional**: `state.metadata.warnings.append(...)` (orchestrator.py:250) fires at Step 3, before grounding/standardization/execution (Steps 4-6) and before the `except Exception` handler at the bottom of `translate()` — so even if a later step throws, the already-appended warning survives on `state.metadata` and reaches `TranslationResult`. Route layer (`translation.py:669`) calls `_build_warnings_list(result)` unconditionally regardless of `result.success`, and `TranslationResponse.warnings` has no `verbose`-gate anywhere in its construction — confirmed by reading the route handler directly, not inferring from the docstring.
- **`state.metadata` is never actually `None` at the point Case 1's guard checks it**: `SessionManager.create_session()` (state.py:107-112) calls `state.initialize_metadata()` unconditionally at session creation, before `translate()`'s body runs. The `and state.metadata` guard is defensive-but-provably-always-true in the current call graph — same "defensive but currently unreachable branch" pattern noted elsewhere in this file (`_organism_display_name()` enum fallback, `excluded_reason` dead-code check) — not a bug, just consistent style with the rest of the file (e.g. line 267's `if state.metadata:` before the defaults/clamps/warnings block uses the identical pattern).
- **`_TEMPERATURE_UNGROUNDED_PREFIX` string-matching verified safe today, but format-dependent and not enforced by type system**: grepped every producer of a `"temperature_celsius"`-keyed warning string codebase-wide — exactly two call sites (`_ground_temperature`'s two `mark_ungrounded()` calls) ever produce the literal `"temperature_celsius: "` prefix in `metadata.warnings`. The multi-step path (`_ground_multi_step_profile`, grounding_service.py:1741-1748) produces a *differently-formatted* string (`f"Step {order} temperature: {reason}"`) for the same underlying condition and is NOT covered by this prefix check — multi-step scenarios with an unresolvable per-step temperature description still get the old un-polished wording ("Could not interpret: '...'" / "No temperature specified", no default-disclosure sentence) and land in the response as generic `type="warning", field=None`. This is a real, verified scope/coverage gap versus the single-step path, not flagged anywhere in the diff's own docs or tests. Acceptable as scoped (the implementation prompt only asked about `_ground_temperature`), but worth surfacing in a follow-up review if multi-step is ever revisited for this same disclosure treatment.
- **New divergence between `AuditSummary.warnings` (verbose audit block, `translation.py:506`, `warnings=list(metadata.warnings)` — raw, unprocessed) and the top-level `TranslationResponse.warnings` (processed via `_build_warnings_list()`)**: before this diff, the two lists were byte-identical for every warning (same raw strings, since `_build_warnings_list` was a straight passthrough of `type="warning"` for everything not already structured, e.g. range clamps come from a *separate* field `metadata.range_clamps`, not from `metadata.warnings`, so they were never duplicated raw). This diff is the first case where the SAME warning string in `metadata.warnings` gets rendered two different ways in the same verbose response body: `audit.audit.warnings` still shows the raw `"temperature_celsius: Temperature description '...' could not be interpreted..."` string (untouched, unstripped), while top-level `warnings` shows the polished/split `WarningInfo(type="unresolved_temperature", field="temperature_celsius", message="Temperature description '...' could not be interpreted...")`. Not a bug (both are individually correct), but an inconsistency a verbose-mode consumer cross-referencing the two lists would notice; not documented anywhere in this diff's spec/doc updates and not covered by any test that asserts `audit.audit.warnings` content for this specific case. Worth flagging in any review that touches `AuditSummary` construction or considers structuring `metadata.warnings` more broadly.
- **Test quality confirmed genuinely non-hollow**: `TestModelTypeFallthroughDisclosure.test_confident_model_type_paths_do_not_warn` (test_full_pipeline.py) — traced `create_scenario()`'s keyword defaults (all flags default `False`, `environmental_conditions` defaults to an empty `ExtractedEnvironmentalConditions()`) against each of the 7 parametrized cases; each case supplies exactly the one signal needed to hit its claimed branch and nothing that would trigger an earlier branch in the priority chain (e.g. `low_ph_condition` sets `ph_value=4.0` and nothing else — no `is_cooking_scenario`/`is_non_thermal_treatment` set — so it genuinely reaches branch 6, not short-circuited earlier). Also confirmed organism-grounding concerns raised in the review brief are moot for this test: model-type determination (Step 3) runs strictly before grounding (Step 4) in `translate()`, so an organism-resolution outcome can never affect whether the fall-through warning fires. `test_unparseable_description_warning_names_the_phrase` / `test_silent_description_warning_differs_from_unparseable` (test_grounding_service.py) call `_ground_temperature()` directly (production code, not a mock) and assert on real returned warning strings — genuine correctness coverage, not testing a mock's own setup. `TestPlainResponseDisclosureWarnings` (test_api_translation.py) is the one test in this batch that's *partially* a plumbing-only check: it hardcodes the exact expected warning strings into `mock_translation_result.state.metadata.warnings` rather than deriving them from real orchestrator/grounding execution, so it verifies route-layer parsing/propagation correctly but does NOT independently verify that production code actually produces those exact strings (that half is covered separately by the grounding unit tests and the `test_full_pipeline.py` fall-through test) — appropriate division of labor across the three test files, not a gap.
- **Docs cross-checked, both accurate**: `specs/specifications.md`'s claim that `_ground_temperature()`'s two model-type-specific defaults come from `settings.default_temperature_abuse_c` (GROWTH/NON_THERMAL_SURVIVAL) vs. `settings.default_temperature_inactivation_conservative_c` (THERMAL_INACTIVATION) verified correct against `app/config/settings.py:184-188` and all 5 read sites in `standardization_service.py`. This also corrects an earlier (pre-2026-08-17) version of this memory file that had claimed these defaults were "hardcoded in the service, not from settings" — that was wrong; see the Conservative Defaults section at the top of this file for the corrected version. `docs/ptm_context.md` §8.15 and `specs/lessons.md`'s 2026-08-17 entry both independently re-verified against the code and are faithful, not aspirational.
- **No safety-critical regression**: `_determine_model_type()`'s actual branch order, defaults, and return values are byte-identical except for the reason string being pulled from a named constant instead of an inline literal (confirmed via diff — the only functional line change is `return ModelType.GROWTH, "default (...)"` → `return ModelType.GROWTH, _MODEL_TYPE_FALLTHROUGH_REASON`, same string value). `WarningInfo.type` remains `str` (not a `Literal`), so adding `type="unresolved_temperature"` is additive, not a breaking schema change — confirmed against `app/api/schemas/translation.py:88`.

## Multi-Step Duration-Clarification Recon, Part 2 (implementation) — 2026-08-17
Clean diff, zero findings above LOW. Confirms Part 1's report-only recon (documented in `specs/lessons.md` 2026-08-17 "Multi-step organism clarification already works") was accurately scoped before this implementation started — worth noting as a positive example of recon-before-design paying off exactly as intended, not just an aspiration.

- **Change**: `GroundedStep` (grounding_service.py) gains two new optional dataclass fields, `duration_phrase: str | None = None` / `temperature_phrase: str | None = None`, appended at the very end of both the dataclass and `add_step()`'s signature (after `dur_provenance`). Populated unconditionally (both resolved and unresolved paths) in `_ground_multi_step_profile()` from `step.duration.description` / `step.temperature.description` — verified by reading the single `grounded.add_step(...)` call site (line ~1772) sitting *after* both the temp-warning and duration-warning `if ... is None:` blocks, not nested inside either. Purely additive; no consumer reads the new fields yet (grepped `duration_phrase|temperature_phrase` codebase-wide — only the 2 production lines + tests + the 2 doc files touched in this same diff).
- **Call-site safety verified exhaustively**: grepped every `add_step(` (2 production, 1 in `test_orchestrator.py`, ~14 in `test_multistep_grounding.py`) and every `GroundedStep(` (1, the internal construction inside `add_step()` itself, keyword-only). All existing callers use either keyword args or positional args that stop before the 2 new trailing params — none could silently receive a shifted value. No other direct `GroundedStep(...)` construction exists anywhere else in the codebase that would need the same treatment.
- **No `__eq__`/serialization risk**: `GroundedStep` is a plain `@dataclass` (auto-generated `__eq__` would now include the 2 new fields), but grepped for any `== GroundedStep`/`steps[N] ==` equality comparisons and any `asdict()`/`astuple()` use on it — none exist. Every consumer (`orchestrator.py`, `translation.py`, `standardization_service.py`) accesses named attributes directly (e.g. `s.step_order`), so additive fields are invisible to them until a consumer is written to read the new ones. Confirmed `translation.py` builds `StepInput`/`StepPrediction` schema objects by explicit named-kwarg construction, not by unpacking/serializing `GroundedStep` wholesale — so the two new fields do NOT leak into the API response even though nothing gates them.
- **Test-premise verification against the real resolver, not assumed**: `test_temperature_phrase_also_carried` uses `"a bit weird"` as an unresolvable temperature phrase — independently ran `find_temperature_interpretation_with_fallback("a bit weird")` directly (not just read the code) and confirmed it returns `None`, so the test's premise is real, continuing the discipline named in the pre-existing lessons.md entry about `"a bit warm"` deceptively resolving via a substring rule (`specs/lessons.md`, referenced in this file's Vague Temperature Workstream section). `"xyz123"` (duration) and `"overnight"` (resolved-via-rule duration) are both already-established fixtures elsewhere in this test suite.
- **Doc accuracy verified against code, not trusted from the diff's own prose**: `specs/lessons.md`'s claim that `Orchestrator._handle_missing_required()` has "zero reference to `is_multi_step`/`has_steps`" — read the full method body (orchestrator.py:521-~610+); confirmed true, trigger is purely `"organism" in std_result.missing_required` + `failure.stage in _CLARIFIABLE_ORGANISM_STAGES`, no step-count dependency anywhere. `specs/specifications.md`'s claim that population is unconditional and that the phrase was "previously read only to build the warning string, then discarded" both verified against the actual code (lines 1761-1780).
- **Verification independently reproduced**: `pytest tests/unit/test_multistep_grounding.py -q` → 40 passed (4 new tests: unresolved-duration-phrase-survives, resolved-via-rule-phrase-also-carried, temperature-phrase-carried, no-description-leaves-None — genuinely distinct assertions, not one test copy-pasted). `black --check` + `ruff check` on both touched Python files → clean, 0 findings.
- **Minor/NIT only**: `_ground_multi_step_profile()`'s docstring (lines 1731-1743) was not updated to mention the new phrase-population behavior — a reader of just the docstring wouldn't know `duration_phrase`/`temperature_phrase` exist. Low impact since the dataclass field comment (lines 330-338) carries the explanation instead. Worth a one-line docstring mention if this method's docstring is touched again for any other reason.

## Multi-Step Duration Clarification Gate — Part 3, the gate itself (2026-08-17)
Reviewed and verified: one confirmed HIGH (audit duplication, empirically reproduced), rest clean. This is the implementation that consumes Part 2's `GroundedStep.duration_phrase`/`temperature_phrase` (see entry above) and the recon documented in lessons.md.

- **CONFIRMED HIGH BUG (empirically reproduced, not just traced): `state.metadata.defaults_imputed`/`range_clamps`/`warnings` get DUPLICATE entries on the duration-gate's successful reply round-trip**, whenever any field other than the just-answered duration(s) needed a default/clamp in that scenario (pH, water activity, initial_inoculum, factor4, or ANY step's temperature — not just the step whose duration was missing). Root cause: `orchestrator.py:314` calls `standardize()` once unconditionally every `translate()` invocation and unconditionally extends `state.metadata` from it at `orchestrator.py:318-322` (this is the pre-existing 2026-05-07 "Audit Completeness Fix" discipline — deliberately unconditional so a hard-FAILED response still has a complete audit). The NEW duration-gate reply-success path (`orchestrator.py:818-823`, inside `_handle_missing_required`) then calls `standardize()` a SECOND time on the same `grounded` within the SAME `translate()` call and extends `state.metadata` AGAIN from that second result. None of `_get_ph`/`_get_water_activity`/`_get_initial_inoculum`/`_get_factor4`/the per-step temperature-default block in `_build_multi_step_profile()` ever mutate `grounded` when they default a value (confirmed by reading all four — they read `grounded.get(...)`/`gs.temperature_celsius` into a local var and never write back), so the second `standardize()` call re-derives and re-appends the *identical* `DefaultImputed`/`RangeClamp` objects for every field that was already defaulted/clamped in the first call. Reproduced live: ran `TestDurationClarificationGate`'s own exact `_scenario()` fixture (from `tests/integration/test_full_pipeline.py`, both step temperatures explicit at 25.0/4.0, no inoculum supplied) through two real `orchestrator.translate()` calls (ask, then a valid full reply) — `result2.metadata.defaults_imputed` came back as `['initial_inoculum_log_cfu', 'initial_inoculum_log_cfu']`, i.e. the exact shipped test scenario already triggers this, the shipped test just never asserts on `defaults_imputed` so it passes. A second repro with step 1's temperature also left unset showed `['initial_inoculum_log_cfu', 'temperature_celsius (step 1)', 'initial_inoculum_log_cfu', 'temperature_celsius (step 1)']`. **Why this couldn't happen for the pre-existing organism gate**: `standardize()`'s organism check (`standardization_service.py:160-163`) returns immediately on organism-missing, strictly before any ph/aw/inoculum/factor4/temperature processing runs — so the FIRST standardize() call in an organism-missing scenario has an empty `defaults_imputed`/`range_clamps`, and only the post-resolution re-run (`orchestrator.py:675-680`, inside `_resolve_organism_from_transcript`) ever populates them, exactly once. The duration gate is structurally different: ph/aw/inoculum/factor4 (and, since the Part 1/2a "run to completion" change, every OTHER step's temperature too) are already fully processed and recorded on the FIRST call even while duration is still missing, so the second call's re-processing is pure duplication, not first-time population. Does not affect the actual prediction/payload values (both calls compute the same correct value) — this is an audit-trail-only defect, but the audit is explicitly safety-critical infrastructure in this codebase (provenance/defaults are the thing a reviewer trusts to know what was assumed), and a duplicated entry misrepresents how many independent assumptions were made. Suggested fix given to the implementer: dedupe by content equality before extending in the two reply-success branches (`orchestrator.py:818-823` and the identical pattern at `orchestrator.py:675-680` is NOT affected, only the duration one is) — `DefaultImputed`/`RangeClamp` are plain Pydantic `BaseModel`s with no identity/timestamp fields, so `==` comparison is reliable for this; alternatively, don't re-extend from `new_result` at all in that branch and rely solely on the first call's already-unconditional capture, EXCEPT this loses genuinely-new events that can only arise on the second call (e.g. a `ComBaseExecutionPayload` `ValidationError` warning at `standardization_service.py:242-243`, which is only reached once `profile` is non-None) — so dedupe-by-equality is the safer fix, not skip-the-second-extend.
- **Verification method worth repeating**: rather than trusting the trace, built a throwaway pytest file in `tests/integration/` that imported the real fixtures (`orchestrator`, `mock_semantic_parser`, `combase_engine`, `vector_store`, `temp_dir`) directly from `test_full_pipeline.py` (importing fixture functions into another test module's namespace makes them usable as fixtures there too — pytest resolves fixtures by name in the importing module's namespace, not just via conftest.py) and ran two real `orchestrator.translate()` calls end-to-end (no mocks below the LLM layer). Deleted the scratch file immediately after confirming (twice, once against a modified fixture, once against the byte-for-byte production test fixture) — did not leave it in the tree or commit it. This is the same "run it, don't just read it" discipline named twice already in this session's own lessons.md entries (the "about two hours" and "a bit warm" cases) — applied here to verify a suspected NEW bug rather than a design premise.
- **Everything else in this diff verified clean**: `_resolve_duration_reply()` is genuinely `@staticmethod` with zero `self.`/service/LLM references (confirmed by reading the full method body — only reads `duration_reply`/`missing_steps` params and the module-level `_MAX_DURATION_MINUTES` constant). All-or-nothing enforcement is correctly ordered — `_resolve_duration_reply()` returns a fully-formed `_DurationReplyResolution` (values=None on any mismatch) *before* the orchestrator's apply-loop (`orchestrator.py:800-816`) ever touches `grounded.steps`, so a rejected reply provably never partially mutates state (traced the exact statement order, not just the docstring's claim). Mutual exclusivity between organism/factor4 and duration gates is real and structural: `standardize()`'s organism check and `_get_factor4()`'s bounds check both `return result` before `_build_multi_step_profile()` is ever reached (`standardization_service.py:161-185` vs. `194`), so `missing_required` can never mix duration entries with organism or factor4 entries — confirmed by reading the control flow directly, not the docstring. The 2a "run to completion" change in `_build_multi_step_profile()` (`standardization_service.py:867-947`, `continue` instead of `return None` on missing duration, `if result.missing_required: return None` moved after the loop) correctly still records `defaults_imputed`/`range_clamps` for a step's temperature even when that SAME step's duration also failed — proven both by a new dedicated unit test (`test_multistep_grounding.py::test_missing_duration_mid_sequence_fails`, deliberately gives step 3 a missing temperature to prove the loop reaches past a mid-sequence duration failure) and independently by my own repro above. Provenance/audit bridging on the success re-entry path is complete: for every answered step, both `GroundedStep.duration_minutes`/`dur_provenance` (mutable object, read by the re-run standardize()) AND the step-qualified `state.metadata.provenance[...]`/`state.grounded_values[...]` entries (which would otherwise stay stale `MISSING` placeholders from `_record_missing_required()`) are updated together (`orchestrator.py:800-816`) — traced the loop body directly. Test quality is genuinely non-hollow: `TestResolveDurationReply` (unit, `tests/unit/test_orchestrator.py`) exercises the real `Orchestrator._resolve_duration_reply()` staticmethod directly with real `GroundedStep`/`DurationClarificationReply` objects (no mocks needed, matching the method's own no-dependency design), covering under-answer, over-answer/stale-step-order, boundary-inclusive (2190.0h exactly), and out-of-range rejection; `TestDurationClarificationGate` (integration) exercises the real orchestrator end-to-end including a genuine `test_partial_reply_fails_closed` that checks `result.state.execution_payload is None` (not just top-level `success`/`status`), and `test_organism_missing_multistep_still_routes_to_organism_gate` proves the reverse-scenario (organism-missing, both durations explicit) still routes correctly to the organism gate with `duration_clarification_question is None`. `_MAX_DURATION_MINUTES` is genuinely derived (not hand-copied) via `annotated_types.Le` introspection of `ExtractedDuration.model_fields["value_minutes"].metadata` — independently verified `le=131400` exists on that field. `ClarificationReason.AMBIGUOUS_DURATION` genuinely existed unused since the very first commit (`0f29025`) — verified via `git log -S`, first real usage is this diff's `clarification_service.py`.
- **LOW**: `annotated_types` is imported directly (`from annotated_types import Le`, `orchestrator.py`) but is not listed as an explicit dependency in `requirements.txt` — only present transitively via `pydantic>=2.6.0`. Low practical risk (pydantic v2 hard-depends on it, current version 0.7.0), but the code now has a direct API dependency on a package requirements.txt doesn't name, which could break silently on an unusual install/vendoring path. Easy fix: add `annotated-types` to requirements.txt.
- **NIT**: `_le_bound = next(m.le for m in ... if isinstance(m, Le))` at module level (`orchestrator.py`, top of file) has no default and will raise a raw `StopIteration` at *import time* (crashing the whole app at startup, not just this feature) if `ExtractedDuration.value_minutes`'s `Field(le=...)` constraint is ever removed or refactored to a custom validator. This is arguably an intentional, desirable fail-fast (consistent with the "derive don't duplicate so they can't drift" design goal explicitly stated in the adjacent comment) rather than a defect — flagging as an observation for the next reviewer to weigh, not a required fix.
- **NIT**: `GroundedStep` is imported as a normal top-level import in `orchestrator.py` (`from app.services.grounding.grounding_service import (GroundedStep, GroundedValues, ...)`) but then referenced as a quoted forward-ref string (`list["GroundedStep"]`) in `_resolve_duration_reply`'s type hint — unnecessary quoting since it's already a real, non-circular top-level import, not a `TYPE_CHECKING`-only one. Harmless, cosmetic only.

## Organism Clarification Goes Free-Text — 2026-08-19 (deleted the LLM reply round trip)
Reviewed and verified clean at CRITICAL/HIGH; two genuine MEDIUM findings (both dead code left behind by an otherwise-thorough simplification), rest LOW/NIT. Confirmed independently: `pytest` on the 5 touched/related test files → 199 passed, 17 deselected (`@pytest.mark.live`); `mypy app` → 151 errors, identical file/line set to the 2026-07-17 and 2026-08-17 baselines (zero new errors, none on touched lines); `black --check` clean on all 10 touched files; `ruff check` on touched files → 18 findings, every one traced to pre-existing lines outside this diff's hunks (same recurring noise set named in prior entries: orchestrator.py E402 import-order + SIM102, metadata.py UP042, translation.py W293/B904, plus 2 new-to-this-list-but-still-pre-existing E402/SIM105 in exp_5_1_clarification_fire_rate.py).

- **Root change**: `Orchestrator._resolve_organism_from_transcript()` went from `async` (called `SemanticParser.extract_clarification_response()`, an LLM round trip) to a plain sync method that runs `transcript.user_reply` directly through `ComBaseOrganism.all_matches_in_text()` — the same deterministic substring-alias matcher a first-turn `pathogen_mentioned` already used. Motivated by a live-confirmed ~50% empty-extraction failure rate on a clean, unambiguous one-word reply ("Salmonellae") across identical runs. Also removed: `ClarificationOption`/`ClarificationQuestion.options`/`ClarificationTranscript.options_offered` (metadata.py), the options-menu construction in `ClarificationService.build_organism_question()` (now prose-only, names 3-4 example pathogens via `_EXAMPLE_PATHOGENS`), and the offered-set membership check in the reply resolver (executability via `registry.is_executable()` is now the sole safety boundary).
- **MEDIUM finding — two now-orphaned public methods left in production code**: `GroundingService.rank_executable_organisms()` (grounding_service.py:1524) and `ComBaseModelRegistry.get_executable_organisms()` (app/engines/combase/models.py:279) were both introduced specifically to build the options menu this diff deletes. Grepped every caller codebase-wide: both now have zero production callers, only their own dedicated unit tests (`test_grounding_service.py`, `test_combase_models.py`) still exercise them directly. The implementer's own doc update (specs/specifications.md:393, verified) explicitly states "no registry/`get_executable_organisms()`/`rank_executable_organisms()` lookup happens here anymore" — so this is a *known*, documented consequence, not an oversight, but the spec's own framing of the change as "a simplification — it deletes... four things" did not extend that deletion discipline to these two methods the way it did for `extract_clarification_response()` (which got an explicit "leave defined, still unit-tested, no production caller" rationale in both code comment and docs). Worth flagging for cleanup or an equally explicit "kept intentionally" rationale if these stay.
- **MEDIUM finding — dead code introduced by the simplification, stale comment**: `Orchestrator._handle_missing_required()`, the `if clarifiable:` round-1-ask branch (orchestrator.py ~695-712), still has an `if clarification is not None: ... ` / `# clarification is None (no viable option set) — falls through to the hard failure below` fallback path. Before this diff, `_build_organism_clarification()` could return `None` for two independent reasons: the failure stage isn't clarifiable, OR the derived option set was empty (`if not ranked: return None`, now-deleted). After this diff, the *only* remaining `return None` in `_build_organism_clarification()` is `if failure is None or failure.stage not in _CLARIFIABLE_ORGANISM_STAGES` — textually identical to the `clarifiable` predicate the caller already checked before entering this branch. Verified `_CLARIFIABLE_ORGANISM_STAGES` (orchestrator.py) and `_STAGE_TO_REASON`'s keys (clarification_service.py, which gates `build_organism_question()`'s only `ValueError`) are the same two-element set by reading both definitions directly — so `clarification is None` is now unreachable, and the comment describing "no viable option set" describes a mechanism (registry-derived ranking) that no longer exists anywhere in the method. Not a functional bug (the dead branch is harmless), but worth a follow-up cleanup pass since it's a stale comment actively describing removed machinery, which is exactly the kind of drift a future reader would be misled by.
- **Test quality confirmed genuinely non-hollow**: `TestResolveOrganismFromTranscript` (test_orchestrator.py) mocks only `registry.is_executable()` (there is nothing else left to mock, matching the method's own no-other-I/O design) and covers all 5 acceptance-criteria shapes from the design doc directly: named-executable resolves, an organism outside the old top-5-by-CDC-deaths menu resolves (Staphylococcus aureus), a reply embedded in a sentence resolves, empty/irrelevant fails closed, ambiguous multi-organism fails closed, non-executable fails closed. `TestClarificationReEntry` (test_full_pipeline.py, integration) adds a genuine `test_named_organism_resolves_deterministically_across_runs` that runs the full `orchestrator.translate()` round trip 6 times with the identical "Salmonellae" reply and asserts success every time — the actual acceptance criterion from the design doc ("6/6 across 6 runs, not 2/6"), not just a unit-level structural proxy for it.
- **Executability check ordering confirmed still correct without the offered-set gate**: `_resolve_organism_from_transcript()`'s fail-closed order is (1) empty match → fail, (2) >1 distinct match → fail (ambiguous), (3) not `registry.is_executable(candidate, model_type, factor4_type)` → fail — traced directly, no offered-set check remains anywhere in the method body. This is airtight as the sole safety boundary: `is_executable()` is re-checked fresh on every call (not trusted from any prior derivation), consistent with the pre-existing A0.5b invariant already documented above in this file.
- **`extract_clarification_response()` confirmed to have no other production caller**: grepped `extract_clarification_response` codebase-wide — the only remaining hits are its own definition (`semantic_parser.py`), its own direct unit tests (`test_semantic_parser.py`), and two docstring/comment mentions in `orchestrator.py`/`test_orchestrator.py` explaining *why* it's no longer called (not actual calls). Both `specs/specifications.md` and `docs/ptm_context.md` were updated to state this explicitly and accurately.
- **API contract change confirmed genuinely breaking on the response side, backward-compatible on the request side**: `ClarificationInfo.options` (a `list[ClarificationOptionInfo]`, required field) was removed from the response schema — a real breaking change for any frontend consumer expecting `options` on an `awaiting_clarification` response; the spec explicitly flags this and scopes the frontend migration out of this diff's responsibility, which is the correct call. On the request side, `ClarificationTranscript.options_offered` was removed, but checked `app/api/schemas/translation.py`/`app/models/metadata.py` for `model_config`/`extra=` settings — neither `TranslationRequest` nor `ClarificationTranscript` sets `extra`, so Pydantic v2's default (`extra="ignore"`) applies: an old client still sending `options_offered` on a request would have it silently dropped, not rejected. Request-side change is non-breaking in the strict validation sense, just semantically inert for stale clients. Both changes are the explicit, reported intent, not defects — worth naming precisely (response breaking / request silently-ignored) for the API-contract review lens.

## Pre-Refactor Characterization Tests — 2026-08-22 (test-only, ahead of engine-relocation refactor)
Reviewed and verified clean. No CRITICAL/HIGH findings. Only LOW/NIT. This is the test-only phase named in specs/lessons.md's own 2026-08-22 entry ("an unverified docstring claim caught by the same discipline it was describing") — that entry's self-correction (water_activity resolves via category-bridge, not plain default, for the `clean_growth` snapshot scenario) was independently re-verified against the committed fixture, not just trusted.

- **Scope**: `tests/unit/test_combase_calculator.py` (+`TestPolynomialCharacterization`, 3 tests pinning `ComBaseCalculator` output against independently-derived floats for Listeria growth / Salmonella thermal inactivation / Salmonella non-thermal survival), `tests/unit/test_model_type_aware_bias.py` (+1 pH-clamp test mirroring the existing temp/aw/factor4 "full triple" shape in `TestRangeClamping`), `tests/unit/test_enums.py` (+`TestEnumMemberSets`, exact `{name: value}` pins for `ModelType`/`Factor4Type`/`EngineType`), `tests/integration/test_response_snapshot_baseline.py` (NEW — full `/api/v1/translate?verbose=true` response-body snapshot for 4 scenarios via real orchestrator/grounding/standardization/engine, only the semantic parser mocked, real-but-empty ChromaDB vector store), `tests/fixtures/expected_outputs.json` (populated from a 0-byte placeholder).
- **Polynomial arithmetic independently re-derived from scratch in a throwaway script (plain `math`, no import of `ComBaseCalculator`) and checked against BOTH the docstring's shown derivation AND the hardcoded `pytest.approx` floats — all three (script, docstring, assertion) agreed to full float precision for all 3 rows.** Also independently pulled the raw `data/combase_models.csv` rows (semicolon-delimited, BOM-prefixed — `encoding="utf-8-sig"`, `delimiter=";"`) and confirmed the coefficients quoted in each docstring match the CSV byte-for-byte, including that b10-b14 are genuinely `0` for the `Factor4Type.NONE` row (not silently defaulted/padded to a wrong value). Cross-read `calculator.py` line-by-line to confirm the actual polynomial term order (`b4=T*pH, b5=T*bw, b6=pH*bw, b7=T², b8=pH², b9=bw²`), the `bw` branch (`aw` directly for THERMAL_INACTIVATION, `sqrt(max(0,1-aw))` for GROWTH and NON_THERMAL_SURVIVAL — the docstrings' claim that only thermal differs is correct), the `mu_max` sign branch (`exp` for GROWTH, `-exp` otherwise), and `calculate_log_increase`'s `mu_max<=0` branch (no `/ln(10)` — mu is already log10-scale for inactivation/survival) — all match the docstrings' stated formulas exactly.
- **"Teeth" claim independently re-verified, not just trusted**: perturbed the Listeria growth (Factor4=NONE) row's b8 coefficient in `data/combase_models.csv` (`-0.31377` → `-0.99999`), reran `test_listeria_growth_pinned` alone, confirmed it failed with the expected float divergence, then `git checkout -- data/combase_models.csv` and reran — clean pass, `git status --porcelain` on the file empty. Did not additionally re-perturb `expected_outputs.json` (implementer already reported doing so and reverting cleanly) since the same verification method was already demonstrated on the CSV path and the fixture-JSON assertion is a plain dict `==` — lower-risk to trust once the pattern is shown sound.
- **Snapshot test mocking/wiring confirmed sound**: `app.api.routes.translation` does `from app.core.orchestrator import ... get_orchestrator` and calls it unqualified (`get_orchestrator()` at translation.py:584) — Python resolves that name from the module's own globals at call time, so `monkeypatch.setattr(route_module, "get_orchestrator", lambda: orchestrator)` genuinely intercepts the real route path (not a no-op patch of an unused reference). Ran the full new/changed test set directly (`pytest tests/unit/test_combase_calculator.py tests/unit/test_enums.py tests/unit/test_model_type_aware_bias.py tests/integration/test_response_snapshot_baseline.py`) — 85 passed, confirming the implementer's reported +11/1418 delta arithmetic (3+1+3+4=11 new tests) independently.
- **Nondeterminism audit (delegated to a sub-agent, cross-checked its citations) found nothing of concern for the 4 pinned scenarios**: no `random`/`uuid`/`time.time()`/`asyncio.gather` anywhere in `app/` (pipeline is strictly sequential/awaited); the handful of `set()` uses in `grounding_service.py` (composite-keyword match, runners-up dedup, category-fallback name dedup) all feed only membership checks, with actual output order coming from an already-ordered list, never from set iteration; `rank_executable_organisms()` (the one `set`-adjacent path with a documented no-production-caller docstring, see the 2026-08-19 entry above) isn't reached anyway since organism is `USER_EXPLICIT` in all 4 scenarios; ChromaDB's empty collection returns zero results regardless of embedding float variance; no numpy/sklearn in the engine/calculator path, so IEEE-754 pure-Python float arithmetic is deterministic across platforms for this case.
- **pH clamp test genuinely mirrors the existing temp/aw shape**: reused the pre-existing `_make_registry_with_constraints(ph_min=..., ph_max=...)` helper (already parameterized for pH, just previously unused by any test) and asserts the same three signals (payload value, `RangeClamp` original/clamped/valid_min/valid_max, warning string mentioning both values) as `test_temperature_above_max_clamped`/`test_water_activity_below_min_clamped`. Not a shallow copy — genuinely exercises the shared `_clamp_to_constraints()` helper path (see the 2026-07-17 factor4 entry above) for the one field (pH) that previously had clamp coverage only implicitly via other tests.
- **Enum member-set tests appropriately scoped for the stated refactor**: they pin `{name: value}` pairs, not module location or import path — exactly what "split into engine-owned vs orchestration-owned enums" (a file-reorganization) is expected to preserve. If the refactor also intends to rename/re-value a member, these tests would correctly fail and force a deliberate re-baseline, which is the explicit characterization-test contract, not brittleness.
- **`water_activity: 1.0` in the `clean_growth` fixture (not `0.99`, the documented conservative-default constant) is correct, not a safety regression**: traced the fixture's own `provenance` block — `water_activity` resolves via `rag_retrieval_category_bridge` to a curated range `[0.99, 1.0]` for the "poultry"/"fresh" category (chicken has no user-supplied aw and the vector store is empty), and `StandardizationService` selects the UPPER bound for GROWTH per the model-type-aware bias direction (more available water = worse for growth) — 1.0 is the correct conservative choice given that curated range, not a bypass of the 0.99 default. `ph` in the same scenario genuinely does fall through to the `conservative_default` 7.0, confirming the category-bridge curated data has aw but not pH for poultry — consistent with the pre-existing note above ("`_index_category_level_rows` correctly handles rows with only aw... and rows with only ph").
- **`mypy`-scope claim verified against `pyproject.toml`**: `[tool.mypy]` has no `files`/`exclude` key at all — its scope is determined entirely by the CLI target (`mypy app`, the documented command in CLAUDE.md), so "tests/ is not mypy's configured scope" is accurate, not a rationalization for skipping a real gate.
- **LOW/NIT only**: `TestPolynomialCharacterization` (test_combase_calculator.py) redefines its own class-local `registry` fixture, byte-identical to the module-level one already defined at the top of the same file — could just rely on the outer fixture instead of shadowing it. `test_response_snapshot_baseline.py`'s `combase_engine` fixture is function-scoped, so all 4 parametrized cases reload `data/combase_models.csv` independently (correctness-neutral, minor redundant I/O) — consistent with `test_full_pipeline.py`'s existing per-test-module local-fixture convention (no shared session-scoped `combase_engine`/`vector_store` fixture exists in `tests/conftest.py` to reuse anyway, so this isn't a missed-reuse smell, just an observation).
