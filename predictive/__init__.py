"""
Predictive microbiology engine library.

Relocated from app/engines/ and app/models/execution/ (plus the engine-owned
enums split out of app/models/enums.py) so it can be consumed as a peer
library by app/ (and, later, other modules). Imports nothing from app.*.
"""
